export const BRADAA_FLAGS = Object.freeze({
  AIPOD: true,
  INTEL: true,
  APPENDIX_REFRESH: true,
  PWA: true,
  TELEMETRY: false,     // default off in dev
  MOCK: true            // allow offline demo
});
window.BRADAA_FLAGS = BRADAA_FLAGS;
